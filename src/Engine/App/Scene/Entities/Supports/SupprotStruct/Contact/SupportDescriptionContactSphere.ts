export class SupportDescriptionContactSphere {
    radius: number;
    deepening: number;//all mm
    
    constructor(radius, deepening) {
        this.radius = radius;
        this.deepening = deepening;
    }
}